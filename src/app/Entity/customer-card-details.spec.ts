import { CustomerCardDetails } from './customer-card-details';

describe('CustomerCardDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerCardDetails()).toBeTruthy();
  });
});
