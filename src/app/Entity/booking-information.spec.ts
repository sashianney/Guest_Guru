import { BookingInformation } from './booking-information';

describe('BookingInformation', () => {
  it('should create an instance', () => {
    expect(new BookingInformation()).toBeTruthy();
  });
});
